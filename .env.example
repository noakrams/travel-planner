# Public browser credentials only. The app uses a local adapter when these are blank.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Never add service-role keys, database passwords, Management API tokens, or GitHub credentials.
